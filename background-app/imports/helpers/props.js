import property from 'lodash.property';

export const propId = property('_id');

export const propOrganizationId = property('organizationId');

export const propNotify = property('notify');

export const propOwner = property('owner');
