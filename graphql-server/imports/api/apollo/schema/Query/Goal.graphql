type GoalsQueryReturnType {
  totalCount: Int!
  goals: [Goal]!
}

type Query {
  # returns a list of goals
  goals(
    organizationId: ID!,
    limit: Int!,
    withDeleted: Boolean
  ): GoalsQueryReturnType!
}
