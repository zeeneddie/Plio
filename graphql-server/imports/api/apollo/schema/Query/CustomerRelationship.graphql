type CustomerRelationshipsQueryPayload {
  totalCount: Int
  customerRelationships: [CustomerRelationship]
}

type Query {
  customerRelationships(organizationId: ID!): CustomerRelationshipsQueryPayload
}