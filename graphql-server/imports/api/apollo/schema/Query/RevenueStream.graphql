type RevenueStreamsQueryPayload {
  totalCount: Int
  revenueStreams: [RevenueStream]
}

type RevenueStreamQueryPayload {
  revenueStream: RevenueStream
}

type Query {
  revenueStreams(organizationId: ID!): RevenueStreamsQueryPayload
  revenueStream(_id: ID!): RevenueStreamQueryPayload
}