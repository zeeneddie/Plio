type CustomerSegmentsQueryPayload {
  totalCount: Int
  customerSegments: [CustomerSegment]
}

type CustomerSegmentQueryPayload {
  customerSegment: CustomerSegment
}

type Query {
  customerSegments(organizationId: ID!, isUnmatched: Boolean): CustomerSegmentsQueryPayload!
  customerSegment(_id: ID!): CustomerSegmentQueryPayload
}
