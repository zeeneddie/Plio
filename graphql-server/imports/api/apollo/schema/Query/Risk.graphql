type RisksQueryPayload {
  totalCount: Int
  risks: [Risk]
}

type RiskQueryPayload {
  risk: Risk
}

type Query {
  risks(organizationId: ID!, isDeleted: Boolean): RisksQueryPayload!
  risk(_id: ID!): RiskQueryPayload!
}
