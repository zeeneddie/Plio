type StandardsQueryPayload {
  totalCount: Int
  standards: [Standard]
}

type StandardQueryPayload {
  standard: Standard
}

input StandardsQueryInput {
  organizationId: ID!
  isDeleted: Boolean
}

type Query {
  standards(input: StandardsQueryInput!): StandardsQueryPayload
  standard(_id: ID!): StandardQueryPayload
}
