{
  "name": "plio",
  "private": true,
  "scripts": {
    "start": "meteor run --settings settings-local.json",
    "debug": "meteor debug --settings settings-local.json",
    "deploy": "DEPLOY_HOSTNAME=galaxy.meteor.com meteor deploy app.pliohub.com --settings settings-local.json"
  },
  "dependencies": {
    "aws-sdk": "^2.4.3",
    "fibers": "^1.0.13",
    "handlebars": "^4.0.5",
    "mammoth": "^1.0.3",
    "meteor-node-stubs": "^0.2.3",
    "path": "^0.12.7",
    "pluralize": "^2.0.0",
    "quill": "^0.20.1"
  }
}
