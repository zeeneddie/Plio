import { PropTypes } from 'react';

export default {
  href: PropTypes.string,
  isActive: PropTypes.bool,
  orgId: PropTypes.string,
  orgName: PropTypes.string,
  orgCreatedAt: PropTypes.string,
  orgOwner: PropTypes.string,
  onClick: PropTypes.func,
};
