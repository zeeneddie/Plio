mutation updateActionOwner($input: UpdateActionOwnerInput!) {
  updateActionOwner(input: $input) {
    action {
      owner {
        _id
        profile {
          fullName
        }
      }
    }
  }
}
