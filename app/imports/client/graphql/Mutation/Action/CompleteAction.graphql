mutation completeAction($input: CompleteActionInput!) {
  completeAction(input: $input) {
    action {
      completionComments
      isCompleted
      completedAt
      verificationTargetDate
      completedBy {
        _id
        profile {
          fullName
        }
      }
    }
  }
}
