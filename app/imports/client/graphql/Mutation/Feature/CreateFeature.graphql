#import "../../Fragment/CustomerElementCard.graphql"

mutation createFeature($input: CreateCustomerElementInput!) {
  createFeature(input: $input) {
    feature {
      ...CustomerElementCard
    }
  }
}