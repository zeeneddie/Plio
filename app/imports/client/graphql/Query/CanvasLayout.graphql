query CanvasLayout($orgSerialNumber: Int!) {
  me {
    _id
    profile {
      fullName
    }
    preferences {
      defaultCanvasColor
    }
  }

  organization(serialNumber: $orgSerialNumber) {
    _id
    name
    lastAccessedDate
  }
}