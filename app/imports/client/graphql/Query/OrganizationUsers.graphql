query OrganizationUsers ($organizationId: ID!) {
  organization(_id: $organizationId) {
    users {
      user {
        _id
        email
        profile {
          fullName
        }
      }
    }
  }
}
