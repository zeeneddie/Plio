query GoalList($organizationId: ID!) {
  goals(organizationId: $organizationId) @connection(
    key: "goalList",
    filter: ["organizationId"]
  ) {
    goals {
      _id
      title
      sequentialId
    }
  }
}