query ValuePropositionList($organizationId: ID!, $isUnmatched: Boolean) {
  valuePropositions(organizationId: $organizationId, isUnmatched: $isUnmatched) {
    valuePropositions {
      _id
      title
    }
  }
}