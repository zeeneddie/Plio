#import "../Fragment/KeyActivityCard.graphql"

query KeyActivityCard($_id: ID!, $organizationId: ID!) {
  keyActivity(_id: $_id) {
    keyActivity {
      ...KeyActivityCard
    }
  }
  user: me {
    _id
    roles(organizationId: $organizationId)
  }
}