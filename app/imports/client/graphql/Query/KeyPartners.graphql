#import "../Fragment/CanvasEntityPresentation.graphql"

query KeyPartners($organizationId: ID!) {
  keyPartners(organizationId: $organizationId) {
    keyPartners {
      ...CanvasEntityPresentation
    }
  }
}