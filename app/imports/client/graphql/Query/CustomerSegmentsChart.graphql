query CustomerSegmentsChart($organizationId: ID!) {
  customerSegments(organizationId: $organizationId) {
    customerSegments {
      _id
      title
      color
      percentOfMarketSize
    }
  }
}