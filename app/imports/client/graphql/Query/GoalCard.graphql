#import "../Fragment/GoalCard.graphql"

query GoalCard($_id: ID!, $organizationId: ID!) {
  goal(_id: $_id) {
    goal {
      ...GoalCard
    }
  }

  user: me {
    _id
    roles(organizationId: $organizationId)
  }
}
