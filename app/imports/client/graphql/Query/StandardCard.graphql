#import "../Fragment/StandardCard.graphql"

query StandardCard($_id: ID!) {
  standard(_id: $_id) {
    standard {
      ...StandardCard
    }
  }
  user: me {
    _id
    profile {
      fullName
    }
  }
}
