query CustomerSegmentList($organizationId: ID!, $isUnmatched: Boolean) {
  customerSegments(organizationId: $organizationId, isUnmatched: $isUnmatched) {
    customerSegments {
      _id
      title
    }
  }
}