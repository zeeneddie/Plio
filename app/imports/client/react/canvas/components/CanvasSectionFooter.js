import styled from 'styled-components';

import { CanvasStyles } from '../constants';

const CanvasSectionFooter = styled.div`
  padding: ${CanvasStyles.sectionPadding};
  display: flex;
  align-items: flex-end;
`;

export default CanvasSectionFooter;
